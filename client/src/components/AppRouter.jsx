import React from "react";
import {Switch, Route, Redirect} from 'react-router-dom';

const AppRouter = () => {
    return (
        <div>
            Router page
        </div>
    );
};

export default AppRouter;